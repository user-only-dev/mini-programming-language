`[]` → empty list<br>
`[Ingredient]` → list <br>
`<list> += [Ingredient]` → add element<br>
`{}` → dictionary(dict)<br>
`{variable:value,var2:val2,...}`<br>
`{data} += {datas}` → add<br>
ready variable:<br>
```
function <>(<name>=<value>) # This is the variable that the function will have if no input is entered. 
```
